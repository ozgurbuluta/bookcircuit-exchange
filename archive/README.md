# Archive

This folder contains legacy code from the Supabase migration.

**Do not use.** The app now runs entirely on Firebase (Auth, Firestore, Storage).

## Contents

- `supabase/` — Original Supabase migrations and config
- `supabase-legacy/` — SQL files that were in the repo root
- `supabase-legacy/src-migrations/` — SQL that was in src/migrations

These are kept for historical reference only. They were archived on 2026-06-21.
